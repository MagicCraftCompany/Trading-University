{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "buildCommand": "prisma generate && next build"
        }
      }
    ],
    "functions": {
      "api/socket/io.ts": {
        "maxDuration": 60
      }
    }
  }