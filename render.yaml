services:
  - type: web
    name: trading-university-websocket
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 4000
      - key: NEXT_PUBLIC_SITE_URL
        sync: false
    autoDeploy: true 